# KernelGuard Python dependencies
#
# NOTE: `bcc` (the BPF Compiler Collection Python bindings) is NOT reliably
# installable via pip - it ships as a system package with native components
# tied to your running kernel version. Install it through your OS package
# manager instead (see README.md "Requirements" section).
#
# Packages below are pip-installable and needed for running the test suite.

pytest>=7.0