# KernelGuard Python controller dependencies
#
# NOTE: `bcc` (the BPF Compiler Collection Python bindings) is NOT reliably
# installable via pip on most distros — it ships as a system package with
# native components tied to your running kernel version. Install it through
# your OS package manager instead; see docs/SETUP.md for exact commands.
#
# Packages below are pip-installable and safe to add to a virtualenv.

# CLI / dev tooling
pytest>=7.0
flake8>=6.0
